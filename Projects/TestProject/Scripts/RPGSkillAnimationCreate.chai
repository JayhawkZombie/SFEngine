def CreateSkillAnimation(this.Skill, FilePath, numFrames)
{
  CreateAnimation(this.Skill, skillAnimation, FilePath, 1)
  
  for(int i = 0; i < numFrames; i++)
  {
    AddFrame(this.Skill, skillAnimation, 0, 16*i, 16, 16)
  }
}
